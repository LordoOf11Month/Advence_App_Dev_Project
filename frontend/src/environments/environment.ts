export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api',
  stripe: {
    publishableKey: 'your_publishable_key_here' // Replace with your actual Stripe publishable key
  }
};
